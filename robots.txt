# https://vinayak682.github.io/robots.txt
#
# This is the ONLY robots.txt on this host that crawlers will ever read.
# A robots.txt is fetched from the domain root and nowhere else, so the copy
# that lives inside the portfolio repo (served at
# /vinayakbhadani.github.io/robots.txt) is invisible to Google and Bing —
# and so was its Sitemap declaration. The Sitemap line below is the one
# that actually gets discovered.

User-agent: *
Allow: /

Sitemap: https://vinayak682.github.io/sitemap.xml

# That file is a sitemap INDEX. It points at the real sitemap, which lives in
# the portfolio repo at /vinayakbhadani.github.io/sitemap.xml. Keeping the
# index at the host root means Search Console and crawlers find a sitemap at
# the conventional location, and the index never needs syncing because it only
# references the other file.
#
# After the domain migration (see migrate-domain.py in the portfolio repo),
# this file moves to vinayakbhadani.com and the line above becomes:
#   Sitemap: https://vinayakbhadani.com/sitemap.xml
